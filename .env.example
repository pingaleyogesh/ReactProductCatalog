# Backend server configuration
SERVER_PORT=5000
FRONTEND_URL=http://localhost:3000

# Stripe configuration (test keys)
STRIPE_SECRET_KEY=sk_test_your_secret_key

# SMTP configuration for GoDaddy mail
# Copy this to a local file named .env and replace placeholders with real credentials.
SMTP_HOST=smtpout.secureserver.net
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=admin@siddhiaqua.shop
SMTP_PASS=your-email-password
FROM_EMAIL=admin@siddhiaqua.shop
ADMIN_EMAIL=pingaleyogesh@gmail.com
